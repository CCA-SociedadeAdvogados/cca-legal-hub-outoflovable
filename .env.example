# Supabase — estas são as únicas variáveis que o frontend precisa
# Copie este ficheiro para .env e preencha com os seus valores reais
# NUNCA faça commit do ficheiro .env (está no .gitignore)
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-public-key"

# ─── Integração Business Central (configurar apenas nos secrets Supabase) ───
# Estas variáveis NÃO são usadas no frontend — são secrets da Edge Function.
# Configurar via: supabase secrets set PA_BC_FLOW_URL="..." PA_API_KEY="..." BC_COMPANY_ID="..."
#
# PA_BC_FLOW_URL   — URL HTTP do Power Automate Flow (trigger)
# PA_API_KEY       — Chave API para autenticar pedidos ao Flow (opcional)
# BC_COMPANY_ID    — GUID da empresa em Business Central
#
# Ver docs/secrets.md para instruções completas.
